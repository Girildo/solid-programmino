# Read by the dev server and by the deployed function. No VITE_ prefix, so it
# is never bundled into client code. Copy to .env.local for local development.
FLICKR_API_KEY=

# Wrangler reads local Function secrets from .dev.vars, not from .env.local.
# Copy this same key there too when using `npm run preview:server`.
